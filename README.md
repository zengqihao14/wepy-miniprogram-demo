# Wepy MiniProgram Demo
> My first wepy-miniprogram

## Build Setup

#### Install wepy-cli

``` bash
$ npm install wepy-cli -g
```

#### Install dependencies

``` bash
$ npm install

# Use Yarn:
$ yarn install
```
#### Build and watching

``` bash
$ wepy build --watch
```
